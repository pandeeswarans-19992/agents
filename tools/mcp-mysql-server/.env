# MySQL connection credentials — copy this file and fill in real values.
# NEVER commit a file that contains real passwords to version control.

DB_HOST=localhost
DB_PORT=3306
DB_NAME=your_database
DB_USER=your_readonly_user
DB_PASSWORD=your_password
